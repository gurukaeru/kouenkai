# CLAUDE.md — 栗原尚希後援会 公式サイト

Claude Code でこのプロジェクトを編集する際は、必ず本ファイルを最初に読み、以下のルールとデザインシステムに従うこと。

## プロジェクト概要

- **目的**: 栗原尚希後援会の公式サイト。政治活動の発信・後援会入会・LINE登録への導線・独自開発アプリの紹介。
- **構成**: `index.html` 1ファイル完結（HTML / CSS / JS 一体型、Tailwind CSS CDN使用）。学習アプリは `apps/` 配下に独立ファイルで設置（例外扱い）
- **デザインコンセプト**: 「書斎の政治」— 装飾ではなく余白・明朝体・細い罫線で品格を出す、編集誌のような知的デザイン。派手な色・光・グラデーションは使わない。動きの質だけで先鋭性を表現する。

### 関連プロジェクトとの関係（重要）

- 栗人サイト（kuryudo.com／`~/Desktop/kuryudo`）は同一人物による自立支援事業のサイトだが、**本サイトとは完全に別プロジェクト**。ファイルを栗人リポジトリに混ぜない。
- デザインシステムも別物：栗人＝クリーム×ゴールド（Shippori Mincho）、本サイト＝紙白×鉄紺（Zen Old Mincho）。トークン・トーンを相互に流用しない。
- 栗人側の CLAUDE.md では政治ページ（kuryudo_politics.html）を「作成しない」と定めている。政治活動の発信はすべて本サイトが担う。
- 報徳思想（至誠・勤労・分度・推譲・循環）は両サイト共通の思想的軸。ここだけは共有してよい。

## 【最重要】公職選挙法ガードレール

**以下は絶対に守ること。違反すると事前運動（公選法129条）等に抵触するおそれがある。**

- ❌ 「立候補予定」「出馬」「〇〇選挙に挑戦」など、本人の立候補に言及する表現を入れない
- ❌ 投開票日・選挙日程への言及、カウントダウン表示を入れない
- ❌ 「清き一票を」「ご支援を」「当選させてください」等の投票依頼表現を入れない
- ❌ 特定の選挙名（南相馬市議会議員選挙 等）と本人を結びつける記述をしない
- ✅ 「政治活動をおこなっています」という表現はOK
- ✅ 後援会への入会案内・LINE登録の呼びかけはOK（政治活動の範囲）
- ✅ アプリ紹介文中の「市議選」「選挙」等は一般的な学習コンテンツの説明なのでOK（本人の立候補と結びつけないこと）
- ⚠️ 本サイトはあくまで「政治活動用」サイト。選挙運動用サイト（候補者名×選挙名×投票依頼の組み合わせ）の体裁に変えない。告示日以降の運用切り替え（選挙運動用への転換・表示義務等）は必ずユーザーが選挙管理委員会等に確認してから行う
- 迷ったら追加せず、コメントで `<!-- 要・公選法確認 -->` を残してユーザーに確認を促す

## デザインシステム

### カラートークン（tailwind.config で定義済み）

| トークン | 値 | 用途 |
|---|---|---|
| `paper` | `#FAF8F2` | 基調背景（書籍用紙の白） |
| `paper2` | `#F2EEE3` | セクション区切りの濃い紙色 |
| `ink` | `#22262E` | 本文（青みの墨色） |
| `tetsu` | `#1B2F4B` | メインカラー（鉄紺・知性と信頼） |
| `tetsu2` | `#2E4A70` | 鉄紺のホバー用明色 |
| `gray1` | `#6E6A61` | 補助テキスト |
| `shu` | `#9E3B2C` | 朱色。**落款ロゴとごく一部の強調のみ**。多用禁止 |

### タイポグラフィ

- 見出し: `font-mincho`（Zen Old Mincho）+ 広め字間（`tracking-widest` 等）
- 本文: `font-jp`（Noto Sans JP）、行間ゆったり（`leading-loose` / `leading-[2.1]`）
- ラベル・肩書き: 極小サイズ + `tracking-[0.4em]` の見出しラベルスタイル

### スタイル原則

- 角丸・ドロップシャドウ・グラデーションは原則使わない（カードのホバー影のみ例外）
- 罫線は `rgba(34,38,46,0.14)` 前後の極細1px（`.rule-t` / `.rule-b`）
- 装飾モチーフは「一筆の円環」（循環の象徴）のみ。新たな装飾を追加しない
- 数字の見出しは漢数字（壱・弐・参・肆…）で統一
- 報徳四綱領（至誠・勤労・分度・推譲）＋循環がブランドの思想的軸

## 実装済みのインタラクション（index.html 内 JS）

1. **Split Text**: `.split` 要素は一文字ずつ blur→鮮明 で出現（`data-split-delay` で開始遅延指定可）
2. **スクロールリビール**: `.reveal` に IntersectionObserver で `.visible` 付与。`data-delay="1|2|3"` で時間差
3. **読了プログレスバー**: `#progress`（画面最上部の極細鉄紺ライン）
4. **ヘッダー質感変化**: スクロール24px超で紙質感（半透明+blur+罫線）が出現
5. **円環パララックス**: 廃止済み（`#enso` は削除。ヒーローは背景透過の本人写真を右上に大きく配置する構図に変更）
6. **スポットライトカード**: `.quiet-card` にマウス追従の薄い光（CSS変数 `--mx`/`--my`）
7. **マグネティックボタン**: `.magnetic` がカーソルに数px追従（タッチデバイス・reduced-motion では無効）
8. **フォームバリデーション**: 必須項目（name, kana, email）+ メール形式 + プライバシー同意

**すべてのアニメーションは `prefers-reduced-motion` を尊重すること。新規アニメーション追加時も必須。**

## ページ構造（セクション順）

1. `#site-header` — 固定ヘッダー（ロゴ・ナビ・SNS・LINE登録ボタン）
2. `#top` — ヒーロー（縦書き見出し左×本人写真右上の2カラム構図。**モバイルでも同構図を維持**。名前・座右の銘・CTA×2は写真の下）
3. `#houtoku` — 報徳思想（四綱領の説明＋市政への落とし込み。編集誌風の縦リスト・罫線区切り）
4. `#apps` — 学びのアプリ4件（壱〜肆の目次風カード）
5. `#message` — 政治姿勢（書簡スタイル・署名付き）
6. `#join` — 後援会入会フォーム
7. footer

※報徳四綱領のマーキー帯（流れる文字の帯）は2026-07-20に削除済み。復活させない

### `#houtoku` セクションの編集ルール

- 四綱領の定義文は、南相馬市公式ウェブサイトの報徳仕法解説（https://www.city.minamisoma.lg.jp/portal/sections/61/6150/61503/study/1/rekishi/houtoku/24995.html）を参照した**独自要約**。市サイトは無断転載不可（引用等を除く）のため、**原文をそのまま転載しないこと**
- 歴史的事実（天明の飢饉→中村藩が報徳仕法を導入→101村で実施、富田高慶）は事実として記述OK
- 「市政への落とし込み」は政治活動としての理念表明。公選法ガードレール（立候補・選挙への言及禁止）を必ず守る

## SEO（実装済み・2026-07-20）

強化キーワード：**「栗原尚希」「南相馬」「南相馬市」**。詰め込みはせず、以下の構造で対応済み。

- title / meta description に「栗原尚希」「福島県南相馬市」を自然に配置
- OGP（og:*）・Twitterカード・canonical を `<head>` に設置
- JSON-LD構造化データ（`@graph`：Organization＋Person＋WebSite。所在地=福島県南相馬市）
- フッターに所在地「福島県南相馬市」を明記（本文には元々ヒーロー・政治姿勢・プロフィールに地名あり）
- `sitemap.xml` / `robots.txt` 生成済み
- favicon一式（`favicon.ico`・`assets/img/common/`）とOGP画像（`assets/img/common/ogp.png`、落款「栗」×鉄紺明朝の仮デザイン）生成済み

**注意**: 本文へのキーワード追加は自然な文脈のみ。隠しテキスト・過剰な地名連呼は行わない（ペナルティ対象）。

### 公開前の必須作業

1. ~~本番URLへの一括置換~~ → **完了（2026-07-21）**。本番URLは `https://kuriharanaoki.com/`（canonical・OGP・JSON-LD・sitemap.xml・robots.txt 設定済み。`CNAME` ファイルも作成済み）
2. Google Search Console にサイト登録し sitemap.xml を送信（公開後）
3. X の Card Validator / Facebookシェアデバッガーで OGP 表示確認（公開後）

## 未対応・今後のタスク（TODO）

- [x] 本人写真の差し替え（背景透過WebP `assets/img/candidate-main.webp` を**左右反転でヒーロー右上に大きく配置**。裾は `.hero-photo` のmaskで紙色にフェード。原本は `assets/img/original/20260619_11.jpg` に保管。差し替え時はVision切り抜き→高さ1000px WebP化の手順を踏襲）
- [ ] SNS各リンクの実URL設定（現在すべて `href="#"`）: X / Instagram / note / LINE
- [ ] LINE公式アカウントの友だち追加URL設定（ヘッダー・ヒーロー・フォーム下・フッターの4箇所）
- [ ] プライバシーポリシーページの作成とリンク設定（フォーム同意チェックのリンク先が現在 `href="#"`）
- [ ] フォーム送信処理の実装（現在フロントのみで、**送信していないのに成功メッセージが表示される状態**。Googleフォーム連携 or サーバーレスAPI を検討。**未実装のまま公開しないこと**）
- [ ] Tailwind CDN（Play CDN）は開発用のため、公開前にビルド版CSSへの置き換え or 継続利用可否を判断
- [ ] 政策セクションの追加（`#message` の後に配置予定。文言はユーザーから支給）
- [x] アプリ4件の体験版設置と実リンク設定（2026-07-20完了。すべて `apps/` 配下・カードからリンク済み・バッジ「体験版を公開中」）
  - 10万人都市シミュレーター → `apps/100k-city.html`（体験版「奇跡の10万人都市」）
  - 市議選ロード → `apps/shigisen-road.html`（体験版「七日間の審判」）
  - 公職選挙法クイズ → `apps/kousenhou-quest.html`（体験版「公選法クエスト」）
  - みえる・わかる地方財政 → `apps/zaisei-quest.html`（体験版「財政クエスト」）
  - 残タスク：正式名称の確定、正式版への差し替え（バッジ「体験版を公開中」→「公開中」）
- [ ] アプリのステータスバッジ更新（準備中 → 体験版を公開中 → 公開中）
- [x] OGP画像・favicon の設定（仮デザイン生成済み。本人写真入り等に変える場合は `assets/img/common/ogp.png` を上書き）
- [x] SEO実装（title/description・OGP・JSON-LD・sitemap/robots。上記「SEO」セクション参照）
- [x] 本番URLの設定（**独自ドメイン `kuriharanaoki.com` 取得済み**。全ファイル置換・CNAME作成済み）
- [ ] GitHub Pages で公開（gurukaeruアカウントに新リポジトリ作成→push→Pages有効化→Custom domainに kuriharanaoki.com 入力→DNS設定（Aレコード185.199.108〜111.153＋www CNAME）→Enforce HTTPS。git初期化・初回コミットはローカルで準備済み）

## 編集ガイドライン

- 差し替え可能な箇所には `<!-- ▼〇〇 ※差し替え可能 -->` 形式の日本語コメントが付いている。編集時もこの形式を維持する
- テキスト変更のみの場合、デザイントークン・アニメーションには触れない
- 新セクション追加時は既存の余白リズム（`py-24`）・見出しラベル様式・reveal クラスを踏襲する
- 1ファイル完結の方針を維持する（画像を除き、外部ファイル分割はユーザー指示があるまで行わない。例外はプライバシーポリシーページのみ）
- 変更後はブラウザで表示・動作確認し、コンソールエラーがないことを確認してから完了報告する
- 公開・デプロイ・GitHub への push はユーザーの明示的な指示があるまで行わない
